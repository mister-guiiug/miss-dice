# Chemin de base de l'app (utile pour GitHub Pages : /<repo>/).
# En dev la valeur par défaut est "/", en build "/miss-dice/".
# Le workflow de déploiement le règle automatiquement depuis le nom du repo.
# VITE_BASE_PATH=/miss-dice/

# Optional: error monitoring via Sentry. Leave empty to disable (no-op,
# the @sentry/react peer is never loaded). Set your DSN to activate.
VITE_SENTRY_DSN=

# Clé de projet PostHog (`phc_…`), nuage EUROPÉEN - la MÊME pour tout le parc :
# un seul projet, les applications distinguées dedans par `app_name`, que le
# socle déduit du chemin de base. Publique par conception (elle est dans le
# bundle). Absente, le bandeau de consentement ne rend rien et rien n'est
# mesuré - c'est le seul interrupteur.
VITE_POSTHOG_KEY=
